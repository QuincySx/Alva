// INPUT:  alva_protocol_acp::protocol::lifecycle
// OUTPUT: pub (re-exported lifecycle types)
// POS:    Re-exports ACP lifecycle protocol types as the single source of truth.
// Re-export from protocol crate — single source of truth
pub use alva_protocol_acp::protocol::lifecycle::*;
