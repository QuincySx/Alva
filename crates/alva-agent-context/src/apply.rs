// INPUT:  alva_types::context::apply
// OUTPUT: re-exports apply_injections, apply_compressions
// POS:    Re-exports apply helpers from alva_types::context::apply.
//! Helpers for applying context hook results to agent state — re-exported from `alva_types::context::apply`.

pub use alva_types::context::apply::{apply_compressions, apply_injections};
