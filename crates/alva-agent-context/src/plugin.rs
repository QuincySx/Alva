// INPUT:  alva_types::context
// OUTPUT: re-exports ContextError, ContextHooks
// POS:    Re-exports the ContextHooks trait and ContextError from alva_types::context.
//! ContextHooks trait — re-exported from `alva_types::context`.

pub use alva_types::context::{ContextError, ContextHooks};
