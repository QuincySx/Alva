[workspace]
members = [
    "src/srow-app",
    "src/srow-core",
    "src/srow-ai",
]
resolver = "2"

[profile.dev]
incremental = true

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
opt-level = 3
strip = true
