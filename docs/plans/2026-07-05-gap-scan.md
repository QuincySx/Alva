# 全仓缺口扫描(2026-07-05)

> **基于**:分支 `fix/subagent-spawn-deadlock-cancel` @ `93edb69`(第一/二波修复 + CLI 三层落地之后)
> **方法**:4 路并行探查——① "Noop 默认未接线"家族全量清查;② Tauri 深扫;③ 第三波积压项核对;④ 未扫角落(memory/context/协议层/协作服务)
> **关联**:[07-02 诊断](./2026-07-02-arch-diagnosis-tech-debt-roadmap.md) · [07-04 进展](./2026-07-04-progress-review-and-next-prs.md)

## TL;DR

**核心病灶已从"死锁/竞态"迁移到两类新形状**:① **结构完整但叶子是 stub** 的子系统(MCP transport、AEP host bridge、Externalize)——测试全绿因为 mock 恰好挡在缺失层前;② **静默失效/静默增长**(Tauri checkpoint 空转、坏 frontmatter 消失、拼错组件 id 无警告、协作层无界累积)。另有两个**必须立刻处理**的事实:20 个 commit 从未推送(单机单点)、session_projection 的**双重计数炸弹条件已全部满足**。阴性结论同样重要:没有第 4 个"危险方向静默放行"的 Noop 实例;子 agent 审批链路在 Tauri 端到端完整;bus 治理健康(7/20);LLM wire 解码路径无裸 unwrap。

## P0 — 立刻处理

| # | 缺口 | 证据 | 动作 |
|---|---|---|---|
| 1 | **20 commit 未推送,CI 从未真跑** | 分支无 upstream;远端 main 停在 `fe4559c`;三份诊断文档未跟踪 | push 分支 + 开 PR;Linux runner 首跑会检验 golden 用例与全套件 |
| 2 | **子 agent 双重计数已上膛** | `session_projection.rs`:`build_turns`(:255)与 token 聚合(:277-285)跑完整父流、不排除 `subagent_run_start/end` 区间;子事件既进父 turns/token 又进 `sub_run` 投影。07-02 预言的延时炸弹,Phase-3 linkage 现已接通=条件全满足 | 聚合时排除 marker 区间(或去重 guard);先实跑一条父调子用例核实量级 |
| 3 | **D-4:send_message 竞态窗口确证** | `tauri/agent.rs:1194→:1231` 跨 4 个 await 无锁无 turn-lock;双窗口并发会把 A 的 prompt 跑在 B 的 session/配置上;reasoning/extra_body 等全局态互相覆盖 | per-agent turn-lock 或 swap+prompt 进同一临界区 |

## P1 — 静默失效(用户会撞上)

| # | 缺口 | 证据 |
|---|---|---|
| 4 | **Tauri 自动检查点静默空转**:`checkpoint` 组件 default_on,但 Tauri 全 crate 无 `set_checkpoint_callback`(CLI 有);middleware 拿不到回调静默 `Ok(())`,无日志 | checkpoint.rs:117-150;CLI agent_setup.rs:145 ✅;Tauri ∅ |
| 5 | **MCP 是结构性空壳**:全仓无可用 transport(活路径 `StubTransport` connect 即败;SSE/WS 结构体未实现 trait;stdio 不存在);开了 mcp 组件=0 工具;命名模板的 mcp_servers 同样拿不到 | mcp/extension.rs:23-45、transport_sse.rs:15-73、skills/extension.rs:106-110 |
| 6 | **两处静默配置吞噬**:坏 skill frontmatter 被 `if let Ok` 丢弃无警告(fs.rs:244,:277);config.json 拼错组件 id 静默忽略(`"shel": false` 关不掉 shell,components.rs:256) | 两个小 PR:加一次性 warn |
| 7 | **D-7:respond_approval 先摘后校验**,失败分支不回插 → UI 消失+工具永久悬挂 | tauri/agent.rs:1100-1117 |
| 8 | **配置洞**:`security` 与 `approval` 独立开关;关 security 留 approval 的 session 里子 agent 危险工具完全无 HITL(子栈 bus 拿不到 guard 就不建 SecurityMiddleware) | agent_spawn.rs:634。**待拍板**:拿不到 guard 时子栈要不要 fail-closed 拒建? |

## P2 — 结构债(第三波队列,经核对全部未动,两处修订)

- **PR-9 权限解析孪生**:两套词汇零交集(空串 Err vs AllowOnce;未知 硬Err vs 静默RejectOnce);security 层无归一目标函数,需新建 str→PermissionDecision(pending_actions.rs:76 的 as_str 可作对偶)。Tauri 侧 parse_decision 仍 0 测试。
- **PR-10 provider 工厂**:5 份 match 分发 + 4 张 base_url 表,规模同诊断。
- **PR-11 ABI 提纯**:agent_session.rs 现 1681 行,行为实现 ~575 行(:520-1098);kernel-abi 其余文件干净,违规集中一处。**D-3 listener 泄漏同 PR 修**(subscribe 返回 guard;ChannelListener "lazy cleanup" 在长命父 session 上累积)。
- **PR-12 compaction:实为三份**——middleware.rs(775)+ compact.rs/auto_compact.rs(751,跨 crate 消费者为零)+ **agent-graph/compaction.rs(第三份,诊断漏计)**。无同挂防护。
- **PR-13 Tauri**:agent.rs 1956 行、24 个 command 全部 0 测试(全 crate 73 个测试集中在 sqlite/provider_api);拆分时连带 D-4/D-7。
- **锁死锁有界化**:`acquire_with_timeout`(scheduler.rs:411)doc 明写该用于热路径,实际零生产调用者;tool_batch.rs:88 仍用无超时 acquire。
- **默认替换契约 0 测试**:builder.rs:192-217 是 AGENTS.md 的核心 opt-out 机制,无任何"同名注册跳过默认"断言。便宜高杠杆。
- **协作层无界增长**:BoardRegistry 无 scope-end 清理、Blackboard 消息无上限、Team inbox 只增不减、Task 输出无限累积;spawn 限深不限宽(children_spawned 计数但不强制)。
- **PR-8 裁决修订**:⚠️ `alva-app-gateway` 已被 Tauri 承重(`start_gateway` :1692)——**从删除名单移除并补进 AGENTS.md**;engine 3 crate / AgentRuntimeBuilder / register_builtin_tools / outcome.rs 维持原判(零消费者,缓刑至 2026-12)。
- **文档漂移**:AGENTS.md 仍写 30 crate(实际 32,缺口正是 llm-wire + gateway);engine 层被描述为活跃;GeminiProvider 漏列。SessionTracker 幽灵已自清。

## P3 — 记录在案

ACP 孤儿清理是打日志的 no-op(orphan.rs);MCP call_tool 无超时且持锁(随真 transport 一起修);AEP host approval/memory 桥仍 Phase-6 stub(组件 default_off);Externalize 压缩动作 no-op;memory embedding 失败静默降级 FTS-only;sqlite 无版本迁移框架、无 busy_timeout;Tauri 不注入项目上下文(CLI-only);主聊天 UI 不渲染子 agent 边界(仅 Inspector);PR-4 目标 `registry.update` 在 Tauri 活路径实为死代码(`#[allow(dead_code)]`)。

## 阴性结论(扫过且干净)

Noop 家族无第 4 个危险实例;子 agent 审批链路 Tauri 端到端完整(bus→notifier→drain→UI→resolve→子 oneshot);ApprovalNotifier fail-closed + warn;CompactionMiddleware/SecurityMiddleware 的 bus 均有 configure() 兜底;WasmSleeper 真身已实现;bus caps 7/20;llm-wire 解码热路径无裸 unwrap。

## 修复记录(2026-07-05 当日)

| 项 | Commit | 说明 |
|---|---|---|
| P0-2 双重计数 | `23b23ef` | RED 实证 110 vs 10;`strip_subagent_ranges` 顶层视图,turns/token 只算父级本层,sub_run 递归自理 |
| P0-3 D-4 + P1-7 D-7 | `bef8f56` | `turn_start_lock` 序列化 ensure→swap→旋钮→prompt;respond_approval 先取 agent 再摘 pending |
| P1-4 checkpoint | `ddb78bd` | CheckpointManager 上移 app-core 共享,Tauri ensure_agent 接线;middleware 缺回调一次性 warn |
| P1-6 静默吞噬 | `12295c2` | frontmatter 解析失败 warn(带路径+错误)+ 容忍性测试;`unknown_toggle_ids()` + apply_components warn |
| P1-5 MCP + P1-8 | `02f605a` | 目录描述写明无 transport;0 工具时汇总 warn;子栈无 guard 时 warn(fail-closed 仍待拍板) |
| P0-1 推送 | (见 git) | 全量绿后推送 + PR |

## 第三波 / 积压清理(2026-07-06)

| 项 | Commit | 说明 |
|---|---|---|
| 默认替换契约测试 | `7be904f` | builder 同名 dedup(AGENTS.md 核心 opt-out)首次获测:3 条契约 |
| 锁超时上热路径 | `b33f9b6` | `ToolLockRegistry::acquire_bounded`(默认 300s),tool_batch 用之;死锁→有界工具错误。原 acquire_with_timeout 零消费者 |
| PR-9 权限解析归一 | `b9fb573` | `PermissionDecision::parse_token`(严格机器)/`parse_interactive`(宽松人类,fail-safe);Tauri/CLI 双壳委托;Tauri 侧首次获测 |
| PR-10 provider 工厂归一 | `f6ffc90` | `build_language_model`/`default_base_url` 两个 canonical 入口;5 份 match+4 张表收敛;**顺手修 openai-responses /v1/v1 404 漂移 bug** |
| AGENTS.md 修正 | `e5e0c20` | 32 crate、llm-wire+gateway 入表、engine 缓刑、Gemini/PR-10 |
| CLI#8 repl dispatch | `d0a63c4` | `dispatch()`+`ReplCtx`+`ReplAction` 提取,5 测;/model 是第 6 份 provider 拷贝(且硬编码 chat)一并归 canonical |
| CLI#9 run_prompt | `931df22` | `accumulate_usage` 纯函数抽出+测;output.rs 整体 writer seam 明确不做(薄格式化壳,低价值) |

**至此:P0/P1 全清 + 第三波 PR-8(gateway 复活修订+文档)/9/10 + 便宜高杠杆项全清。剩余大型结构重构见下。**

## 剩余(未动,大型结构重构 — 需专门会话)

- **PR-11 ABI 提纯**:agent_session.rs 1681 行,行为实现 ~575 行迁往 kernel-core;同 PR 修 D-3 listener 泄漏(subscribe 返回 guard)。破坏性,应赶在 SDK 1.0 前。
- **PR-12 compaction 三份归一**:middleware.rs(775)+ compact.rs/auto_compact.rs(751,跨 crate 零消费者)+ agent-graph/compaction.rs。先设计文档。
- **PR-13 Tauri 拆分**:agent.rs 1956 行 God-file 按职责拆 ~9 模块 + 命令层测试基建(24 命令仍 0 测试)。
- **协作层无界增长**:BoardRegistry/Blackboard/Team inbox/Task output 无 eviction;spawn 限深不限宽。需背压/TTL 设计。
- **P1-8 待拍板**:子栈无 guard 时是否 fail-closed(当前 warn 不拒)。
- P3 杂项(ACP 孤儿清理、MCP call_tool 超时、Externalize、memory embedding 降级、sqlite busy_timeout)随相关子系统推进。

## 建议执行序

**本周**:P0-1 推送开 PR(CI 首跑)→ P0-2 双重计数(先实测再修)→ P1-4 checkpoint 接线(小)→ P1-6 两个 warn(小)→ P0-3/P1-7 合一个 Tauri 竞态 PR。
**下一批**:P1-5 MCP 裁决(接真 stdio transport 或 default_off+文档)、P1-8 拍板、默认替换契约测试、acquire_with_timeout 上热路径。
**随后**:第三波按 PR-13(带 Tauri 测试)→ PR-9 → PR-10 → PR-11(+D-3)→ PR-12(三份归一)推进;AGENTS.md 一次性修正。
